{
  "name": "wifi-crud",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute wifi-crud-db --remote --file=./schema.sql",
    "db:migrate:local": "wrangler d1 execute wifi-crud-db --local --file=./schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260714.0",
    "wrangler": "^4.0.0"
  }
}
